# STEP 09 — 빌드 테스트 + 배포 준비

> 이 단계의 목표: 프로덕션 빌드 성공, 배포 설정 완료

---

## 🔨 프롬프트 (Claude Code에 입력)

```
배포를 위한 최종 점검과 설정을 해줘.

### 1. 빌드 테스트

npm run build를 실행해서 에러를 잡아줘.

흔한 에러들:
- "use client" 누락 (useState, useEffect 쓰는 컴포넌트)
- 서버 컴포넌트에서 클라이언트 훅 사용
- 이미지 최적화 경고
- 타입 에러
- import 경로 에러

빌드 성공할 때까지 에러를 하나씩 수정해줘.

### 2. 빌드 후 로컬 실행

npm run start로 프로덕션 서버 실행해서
dev 모드와 동일하게 동작하는지 확인.

### 3. next.config.js 최적화

- images: 필요시 domains 설정
- output: 'standalone' (선택)
- 불필요한 경고 suppression

### 4. .gitignore 확인

필수 제외:
- node_modules/
- .next/
- .env*
- *.log
- .DS_Store

포함 확인:
- content/ 폴더 (★ 이건 포함해야 함!)
- public/files/ (이력서 PDF 등)

### 5. README.md 작성

프로젝트 루트에 README.md:

# [이름] 포트폴리오

## 소개
화학공학 → 풀스택 → AI Agent 개발자 포트폴리오
타겟별 3개 버전 (개발자/인사팀/구인)

## 기술 스택
- Next.js 14+ (App Router)
- TypeScript
- Tailwind CSS
- Framer Motion
- Content: Markdown (gray-matter)

## 라우트
- / : 라우터 허브 (화학→코드→AI 스크롤 인터랙션)
- /dev : 개발자용 (터미널 스타일, 아키텍처, 기술 의사결정)
- /hr : 인사팀용 (스토리텔링, 임팩트, 커리어 전환)
- /hire : 알바/구인용 (서비스, 프로세스, 문의)

## 콘텐츠 관리
content/ 폴더의 .md 파일을 수정하면 자동 반영됩니다.
- content/profile.md — 프로필 정보
- content/projects/*.md — 프로젝트 추가/수정
- content/career/*.md — 경력 추가/수정
- content/hire/services.md — 서비스 목록

## 로컬 실행
npm install
npm run dev

## 배포
Vercel 연동 후 자동 배포

### 6. Vercel 배포 가이드

배포 옵션 2가지를 알려줘:

Option A — Vercel CLI:
- npm i -g vercel
- vercel 명령어로 바로 배포
- 환경변수 필요 없음 (정적 사이트)

Option B — GitHub 연동:
- GitHub 저장소 push
- vercel.com에서 Import Project
- 자동 빌드/배포

둘 다 간략한 순서 작성.
```

---

## ✅ 체크포인트 (Claude Code에 입력)

```
STEP 09 빌드 + 배포 준비 확인해줘:

1. 빌드 테스트:
   npm run build
   → 에러 없이 성공하는지
   → 경고가 있다면 내용 확인

2. 프로덕션 실행:
   npm run start
   → http://localhost:3000 접속
   → 4개 페이지 모두 정상인지 빠르게 확인
   → dev 모드와 차이 없는지

3. 파일 점검:
   - .gitignore에 node_modules, .next 제외되는지
   - content/ 폴더가 .gitignore에 포함되지 않는지 (포함되어야 함!)
   - README.md 존재하는지

4. 빌드 사이즈:
   - .next 폴더 크기 확인 (비정상적으로 크지 않은지)
   - 번들 사이즈 경고 없는지

5. TypeScript 최종 확인:
   npx tsc --noEmit

모두 통과하면 "STEP 09 ✅ 완료 — 배포 준비 완료!"라고 알려줘.
```

---

## 🔍 이 단계에서 확인할 것

| 항목 | 기대 결과 |
|------|----------|
| `npm run build` | 에러 0 |
| `npm run start` | 4개 페이지 정상 |
| .gitignore | content/ 포함, node_modules 제외 |
| README.md | 존재 + 내용 적절 |

→ 전부 통과하면 **STEP-10.md** (선택) 또는 배포!
